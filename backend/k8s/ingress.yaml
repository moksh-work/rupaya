apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rupaya-backend-ingress
  annotations:
    kubernetes.io/ingress.class: alb
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rupaya-backend
                port:
                  number: 3000
