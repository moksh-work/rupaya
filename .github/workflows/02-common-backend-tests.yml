name: 02 - Backend Tests & Lint

# DEPRECATED: Backend tests consolidated into Workflow 01
# This workflow is kept as a placeholder for future backend-specific CI additions
# Path filters remain to enable selective re-activation if needed

on:
  push:
    branches:
      - main
      - feature/*
    paths:
      - 'backend/**'
      - 'shared/**'
      - '!docs/**'           # Skip if only docs changed
      - '!*.md'              # Skip if only README/docs changed
  pull_request:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'shared/**'
      - '!docs/**'
      - '!*.md'

jobs:
  # Tests have been consolidated into workflow 01 - Code Validation & Linting
  # This workflow now serves as a placeholder for future backend-specific CI steps
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Note
        run: |
          echo "✅ Backend tests are run by Workflow 01 (Code Validation & Linting)"
          echo "This workflow is available for future backend-specific CI additions"
